# Content Ingestion

Use this folder for raw user-provided source files that should be kept in-repo but not publicly served.

## Flow
1. Drop raw files in `docs/content/inbox/`
2. Optimize/crop/convert assets
3. Publish final assets to `public/content/`

Public site assets should live only in `public/content/`.
